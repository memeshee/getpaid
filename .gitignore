# Files and directories created by pub
.dart_tool/
.packages

# Pubspec overrides
pubspec_overrides.yaml

# Agents
.dart_skills/
.agents/
.claude/
.gemini/
.cursor/
.vscode/mcp.json
.codex/
.mcp.json

# Serverpod Cloud CLI
.scloud/

# Serverpod secrets — NEVER commit (generated per environment)
getpaid_server/config/passwords.yaml

# Built Flutter web app (regenerate with: flutter build web in getpaid_flutter,
# then copy build/web to getpaid_server/web/app)
getpaid_server/web/app/

# Local IDE — may carry machine-specific secrets
.vscode/
